import React from "react";

const Hero = () => (
	<div className="relative h-64 md:h-96 bg-gray-100 mb-8 rounded-lg overflow-hidden">
		<div className="absolute inset-0 flex items-center justify-center">
			<div className="text-4xl md:text-6xl font-bold text-primary">R</div>
		</div>
	</div>
);

export default Hero;
